ErrorResponse:
  required:
    - message
  properties:
    message:
      type: string

# Group 1
Entity:
  properties:
    # TODO!!!
    id:
      type: integer
      format: int32
    property2:
      type: string
    property3:
      type: string

UpdateEntity:
   properties:
    # TODO!!
    property1:
      type: string
    property2:
      type: string

Artist:
  properties:
    # TODO!!!
    id:
      type: integer
      format: int32
    property2:
      type: string
    property3:
      type: string

UpdateArtist:
   properties:
    # TODO!!!
    property1:
      type: string
    property2:
      type: string

Instrument:
  properties:
    # TODO!!!
    id:
      type: integer
      format: int32
    property2:
      type: string
    property3:
      type: string

UpdateInstrument:
   properties:
    # TODO!!!
    property1:
      type: string
    property2:
      type: string

Release:
  properties:
    # TODO!!!
    id:
      type: integer
      format: int32
    property2:
      type: string
    property3:
      type: string

UpdateRelease:
  properties:
    # TODO!!!
    id:
      type: integer
      format: int32
    property2:
      type: string
    property3:
      type: string

Work:
  properties:
    # TODO!!!
    id:
      type: integer
      format: int32
    property2:
      type: string
    property3:
      type: string

UpdateWork:
  properties:
    id:
      type: integer
      format: int32
    property2:
      type: string
    property3:
      type: string

# Group 2
WebsiteMetadata:
  properties:
    id:
      type: integer
      format: int32
    downloadPath:
      type: string
    url:
      type: string
    occurrences:
      type: array
      items:
        $ref: '#/definitions/Occurrence'

WebsiteContent:
  properties:
    content:
      type: string

UpdateWebsite:
  properties:
    url:
      type: string
    occurrences:
      type: array
      items:
        $ref: '#/definitions/Occurrence'
    content:
      type: string

Occurrence:
  properties:
    entity:
      type: string
    role:
      type: string
    positions:
      type: array
      items:
        type: integer
        format: int32
