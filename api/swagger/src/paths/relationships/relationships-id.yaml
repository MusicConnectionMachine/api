x-swagger-router-controller: relationships
get:
  description: Retruns relationship with specified ID
  operationId: getRelationshipByID
  parameters:
    - name: id
      in: path
      description: ID of Instrument
      required: true
      type: integer
  responses:
    "200":
      description: Success
      schema:
        $ref: '#/definitions/Relationship'
    default:
      description: Error
      schema:
        $ref: "#/definitions/ErrorResponse"
put:
  description: Update relationship
  operationId: updateRelationship
  parameters:
    - name: apiKey
      in: query
      description: API key
      required: true
      type: string
    - name: id
      in: path
      description: ID of instrument
      required: true
      type: integer
    - name: body
      in: body
      description: instrument data
      required: true
      schema:
        $ref: '#/definitions/UpdateRelationship'
  responses:
    "200":
      description: Success
      schema:
        $ref: '#/definitions/Relationship'
    default:
      description: Error
      schema:
        $ref: "#/definitions/ErrorResponse"
delete:
  description: Delete relationship
  operationId: deleteRelationship
  parameters:
    - name: apiKey
      in: query
      description: API key
      required: true
      type: string
    - name: id
      in: path
      description: ID of Relationship
      required: true
      type: integer
  responses:
    "200":
      description: Success
    default:
      description: Error
      schema:
        $ref: "#/definitions/ErrorResponse"
